name: 'Scorecard'

on:
  push:
    branches:
      - main
  schedule:
    # Weekly on Saturdays.
    - cron: '30 1 * * 6'

# Default-deny token permissions; the analysis job below grants the extra scopes it needs.
permissions:
  contents: read

jobs:
  analysis:
    name: 'Scorecard analysis'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      # Needed for the Code Scanning upload.
      security-events: write
      # Needed for the GitHub OIDC token used by publish_results.
      id-token: write
    steps:
      - name: Harden Runner
        # Audit mode first; switch to `egress-policy: block` with an endpoint allowlist after a few real runs.
        uses: step-security/harden-runner@05e31511f85b41b11d1cf0ef85d0992719546e2c # v2.21.0
        with:
          egress-policy: audit

      - name: 'Checkout code'
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: 'Run analysis'
        uses: ossf/scorecard-action@4eaacf0543bb3f2c246792bd56e8cdeffafb205a # v2.4.3
        with:
          results_file: results.sarif
          results_format: sarif
          # This repository is public: publishing feeds the Scorecard public dataset
          # (https://github.com/ossf/scorecard#public-data) and enables the results badge.
          publish_results: true

      - name: 'Upload artifact'
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: SARIF file
          path: results.sarif
          retention-days: 5

      - name: 'Upload to code-scanning'
        uses: github/codeql-action/upload-sarif@54f647b7e1bb85c95cddabcd46b0c578ec92bc1a # v4.36.3
        with:
          sarif_file: results.sarif
